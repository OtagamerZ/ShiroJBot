{
  "name": "Shiro J. Bot",
  "description": "Uma bot meiga e um tanto rígida, seja qual for sua necessidade Shiro pode lhe ajudar!",
  "keywords": [
    "JDA",
    "Discord",
    "Bot"
  ],
  "repository": "https://github.com/OtagamerZ/ShiroJBot",
  "logo": "https://images-ext-2.discordapp.net/external/VEvNfPPxZPcXdnO-erMqFpB5D7tCXtPiTPOKt3chwqM/https/cdn.discordapp.com/avatars/572413282653306901/081151d97708b2e90b45927136abb432.png",
  "env": {
    "SERVER_IP": {
      "description": "IP do servidor.",
      "required": true
    },
    "SERVER_URL": {
      "description": "URL do servidor.",
      "required": true
    },
    "BOT_TOKEN": {
      "description": "Token de acesso do bot principal.",
      "required": true
    },
    "DB_LOGIN": {
      "description": "Login de acesso do banco de dados.",
      "required": true
    },
    "DB_PASS": {
      "description": "Senha de acesso do banco de dados.",
      "required": true
    },
    "TOPGG_TOKEN": {
      "description": "Token de acesso à API do Top.gg",
      "required": false
    },
    "DBL_TOKEN": {
      "description": "Token de acesso à API do DiscordBots List",
      "required": false
    },
    "JIBRIL_TOKEN": {
      "description": "Token de acesso do bot secundário (Relay).",
      "required": true
    },
    "JIBRIL_LINK": {
      "description": "Link de convite do bot secundário (Relay).",
      "required": true
    },
    "YOUTUBE_TOKEN": {
      "description": "Token de acesso à API do YouTube.",
      "required": true
    },
    "NOWANIMES_URL": {
      "description": "URL da API do Now Animes.",
      "required": false
    },
    "NOWANIMES_TOKEN": {
      "description": "Token de acesso à API do Now Animes.",
      "required": false
    },
    "MEGAHENTAIS_URL": {
      "description": "URL da API do Mega Hentais.",
      "required": false
    },
    "MEGAHENTAIS_TOKEN": {
      "description": "Token de acesso à API do Mega Hentais.",
      "required": false
    },
    "SECRET": {
      "description": "Frase secreta para ser usada no comando TheAnswerCommand.",
      "required": true
    },
    "TET_TOKEN": {
      "description": "Token de acesso do bot secundário (RPG).",
      "required": true
    },
    "TET_LINK": {
      "description": "Link de convite do bot secundário (RPG).",
      "required": true
    },
    "GOOGLE_TOKEN": {
      "description": "Token de acesso à API do Google.",
      "required": true
    },
    "GOOGLE_SEARCH": {
      "description": "Código de Custom Search do Google.",
      "required": true
    },
    "BOT_SECRET": {
      "description": "Chave de uso na autenticação oAuth2 do Discord.",
      "required": true
    },
    "CARDS_PATH": {
      "description": "Caminho para a pasta onde encontram-se os arquivos de imagem das cartas Kawaipon.",
      "required": true
    },
    "COLLECTIONS_PATH": {
      "description": "Caminho para a pasta onde serão armazenados os arquivos de imagem das coleções Kawaipon.",
      "required": true
    },
    "TWITCH_TOKEN": {
      "description": "Token de acesso à API da Twitch.",
      "required": false
    }
  }
}
